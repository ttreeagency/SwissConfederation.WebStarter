prototype(WebStarter:Home.Document) < prototype(WebStarter:Default.Document) {
    prototype(WebStarter:Container.ContentArea) {
        header = Fusion:Array {
            personList = WebStarter:Object.PersonList
            title = WebStarter:Object.PageTitle
        }
    }
}
